# 🗺️ نقشه راه و ترتیب تدریس پروژه به کارآموزان

این سند ترتیب بررسی فایل‌های پروژه را هنگام کدنویسی فرانت‌اند مشخص می‌کند تا مفاهیم به‌صورت زنجیره‌وار و منطقی به کارآموزان منتقل شوند.

---

### 🟢 فاز اول: ساختار پایه و چیدمان اصلی (Scaffolding & Layout)

در این بخش، کارآموز با ساختار پوشه‌ها و نحوهٔ رندر شدن اولین خروجی‌ها آشنا می‌شود.

1. **`app/layout.jsx`**
   - **مفهوم آموزشی:** تعریف ساختار کلی صفحات (`html` و `body`)، اضافه کردن فونت‌های فارسی، استایل‌های عمومی (`Global CSS`) و مفهوم Layout در Next.js.
2. **`app/page.jsx`**
   - **مفهوم آموزشی:** صفحهٔ اصلی (Landing Page)، ساختار درختی کامپوننت‌ها و نحوهٔ import کردن بخش‌های مختلف.
3. **`app/globals.css`**
   - **مفهوم آموزشی:** استایل‌های سراسری، فونت فارسی، جهت راست‌به‌چپ و تفاوت CSS سراسری با کلاس‌های Tailwind.
4. **`package.json`، `next.config.ts` و `tailwind.config.ts`**
   - **مفهوم آموزشی:** وابستگی‌ها، اسکریپت‌های پروژه، پیکربندی Next.js و طراحی Utility-first با Tailwind CSS.

---

### 🟡 فاز دوم: کامپوننت‌های رابط کاربری و نمایش داده‌ها (UI & Props)

در این بخش، کارآموز یاد می‌گیرد کدهای تکراری را به کامپوننت‌های قابل استفادهٔ مجدد تبدیل کند.

5. **`components/MovieCard.jsx`**
   - **مفهوم آموزشی:** پاس دادن داده از طریق `props`، رندر اطلاعات پویا، نمایش تصاویر واکنش‌گرا و استفاده از تصویر جایگزین (Fallback Image).
6. **`components/HeroBanner.jsx`**
   - **مفهوم آموزشی:** استفاده از پکیج آیکون `lucide-react`، استایل‌دهی با Tailwind CSS، افکت‌های هاور و ساخت بنر نمایشی.
7. **`components/Footer.jsx` و `components/Sidebar.jsx`**
   - **مفهوم آموزشی:** ساخت کامپوننت‌های مشترک، ترکیب‌بندی رابط کاربری و لینک‌های سادهٔ HTML.
8. **`components/Rating.jsx` و `components/LikeButton.jsx`**
   - **مفهوم آموزشی:** ساخت کنترل‌های تعاملی کوچک و آماده‌سازی UI برای مدیریت وضعیت کاربر.

---

### 🔵 فاز سوم: ناوبری و جابه‌جایی بین صفحات (Routing & Navigation)

کارآموز یاد می‌گیرد چگونه بدون بارگذاری کامل صفحه، بین مسیرهای مختلف جابه‌جا شود.

9. **`components/Navbar.jsx`**
   - **مفهوم آموزشی:** استفاده از کامپوننت `<Link>`، مسیرهای داخلی، جست‌وجوی لحظه‌ای و بهینه‌سازی جابه‌جایی صفحات در Next.js.
10. **`components/Search/SearchResultItem.jsx`**
    - **مفهوم آموزشی:** ساخت آیتم نتیجهٔ جست‌وجو، لینک پویا و بارگذاری غیرهم‌زمان تصویر.
11. **`app/movies/[slug]/page.jsx`**
    - **مفهوم آموزشی:** Dynamic Routing، خواندن پارامتر از آدرس (`params.slug`)، `notFound` و تولید metadata برای صفحهٔ جزئیات.

---

### 🟠 فاز چهارم: مدیریت وضعیت و فرم‌ها (State & Forms)

در این مرحله، کارآموز وارد بخش‌های تعاملی سایت و مدیریت داده‌های واردشده توسط کاربر می‌شود.

12. **`components/CommentSection.jsx`**
    - **مفهوم آموزشی:** `useState`، فرم کنترل‌شده، اعتبارسنجی اولیه، `useMemo` و مدیریت فهرست نظرات.
13. **`components/Rating.jsx` و `components/LikeButton.jsx`**
    - **مفهوم آموزشی:** تغییر state با رویدادهای کلیک و تغییر ظاهر رابط بر اساس وضعیت فعلی.
14. **`hooks/useMoviesWithTmdb.js`**
    - **مفهوم آموزشی:** ساخت Custom Hook، `useEffect`، درخواست داده و ترکیب state محلی با اطلاعات سرویس خارجی.
15. **`app/HomeContent.jsx`**
    - **مفهوم آموزشی:** Client Component، فیلتر و گروه‌بندی آرایه‌ها، `map` و رندر شرطی بخش‌های مختلف صفحه.

---

### 🔴 فاز پنجم: داده، API و اتصال به سرویس خارجی (Data & API)

در این بخش، کارآموز با Route Handlerهای Next.js و ارتباط با TMDb آشنا می‌شود.

16. **`data/db.js`، `data/comments.js` و `data/imageMapper.js`**
    - **مفهوم آموزشی:** شکل‌دهی دادهٔ اولیه، مدل سادهٔ رکوردها و نگاشت داده برای استفاده در UI.
17. **`lib/tmdb.js`**
    - **مفهوم آموزشی:** ماژولار کردن درخواست‌ها، ساخت URL، متغیرهای محیطی و احراز هویت Bearer.
18. **`app/api/movies/route.js`**
    - **مفهوم آموزشی:** Route Handler، متد `GET` و پاسخ JSON.
19. **`app/api/tmdb/details/route.js` و `app/api/tmdb/images/route.js`**
    - **مفهوم آموزشی:** Query String، دریافت جزئیات و تصاویر، مدیریت خطا و سیاست‌های cache.
20. **`app/api/tmdb/image-proxy/route.js`**
    - **مفهوم آموزشی:** Proxy سمت سرور، `fs` در runtime نود، ذخیرهٔ cache فایل و `AbortController`.

---

### 🟣 فاز ششم: امنیت و دسترسی‌ها (Authentication & Protection)

این بخش برای ادامهٔ آموزشی پروژه پیشنهاد می‌شود و در نسخهٔ فعلی هنوز فایل اجرایی ندارد.

21. **`app/login/page.jsx` (قابل افزودن)**
    - **مفهوم آموزشی:** `useState` و `useRef` برای دریافت دادهٔ ورودی، اعتبارسنجی فرم و نمایش خطا.
22. **`hooks/useAuth.js` یا Context مرتبط (قابل افزودن)**
    - **مفهوم آموزشی:** نگهداری وضعیت Authentication و دسترسی به اطلاعات کاربر در سراسر پروژه.
23. **`middleware.js` (قابل افزودن)**
    - **مفهوم آموزشی:** بررسی توکن یا کوکی قبل از دسترسی به مسیر، Redirect و Route Protection.

---

### تمرین‌های پیشنهادی پس از اتمام مسیر

- ساخت صفحهٔ واقعی `/movies` با فیلتر ژانر و pagination.
- انتقال نظرات از state محلی به پایگاه‌داده و ساخت API برای `POST`.
- افزودن احراز هویت و محافظت از مسیر ثبت نظر.
- بررسی Lazy Loading و Suspense برای تصاویر و بخش‌های سنگین.
